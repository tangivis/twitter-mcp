# Run `uv run pre-commit install` once after cloning to enable.
# Keep `rev` in sync with the ruff version pinned in uv.lock.
repos:
  - repo: https://github.com/astral-sh/ruff-pre-commit
    rev: v0.15.9
    hooks:
      - id: ruff-format
      - id: ruff-check
        args: [--fix]
